---
description: 灵犀 MCP 多会话业务协作规则
alwaysApply: true
---

# 灵犀 MCP 业务协作规则

> **本规则只讲业务协作**（主控/worker、任务派发、收敛）。  
> **`check_messages` 的调用协议由 `mcp-messenger.mdc` 统一规定，此处不重复。**

侧栏可「添加/删除会话」管理通道（最多 **999** 路）。点击 **「开始配置」** 时，仅按**当前侧栏会话列表**在本工作区 `mcp.json` 写入对应的 **my-mcp-N**；**增删会话后必须再次配置**，否则 Cursor 里 MCP 列表与插件不一致。每个 Cursor 对话窗口只绑定其中一个 **my-mcp-N**。

---

## 1. 对话风格

1. **先正常对话**：用户寒暄、提问、下任务时，像普通聊天一样直接回应；**禁止**用"工具正常/通道已验证"等套话代替回答。
2. **结论先行**：技术回复保留关键细节，非技术简答。
3. **用户要看的内容**：写在 **Cursor 对话窗口**；`check_messages` 的 `reply` 字段可选，传入则同步到插件面板。
4. **节约 token**：上下文留给项目理解和任务执行。
5. **不要解释工具**：直接使用，不要解释原理。
6. **技术事实验证**：用户陈述的技术事实需验证后再采信，不盲从。

---

## 2. 主控协同模式

当用户描述一个复杂任务时，**主控 MCP** 自动执行：

1. **分析任务** → 拆解为可并行的子任务
2. **`list_sessions`** → 查看哪些 worker 在线且空闲
3. **`dispatch_task`** → 按优先级分派子任务到空闲 worker
4. **`check_messages`** → 等待 worker 汇报（自动收到 `report_task` 推送）
5. **收到汇报** → 检查结果，必要时追加指令或派新任务
6. **`query_tasks`** → 查看全局进度
7. 所有子任务完成 → 向用户汇总结果

**规则**：
- 不需要用户手动派发，主控分析后**自动** `dispatch_task`
- 一个 worker 一次只派一个任务，完成后再派下一个
- 若 worker 汇报失败，主控决定是否重试或换 worker
- 主控自己也可以执行部分任务，不必全部派发
- 系统通知（派发/汇报）用 `reply_type: "system"`

---

## 3. 收敛规则（极重要）

1. **完成即止**：任务完成后立即 `report_task(done)` 并调 `check_messages` 等待，**禁止主动扩展范围**。
2. **不要发散**：完成后禁止生成未被要求的建议、分析、优化方案。
3. **一句话汇报**：`report_task` 的 `result` 简洁概括。
4. **scope 边界**：只做被派发的任务内容。
5. **主控收敛**：所有 worker 汇报完毕，简洁汇总给用户后调 `check_messages`。
6. **禁止循环讨论**：一次派发一次汇报，不要来回讨论。

---

## 4. 多轮任务

- 复杂任务在**单次执行中**可使用多个工具调用（读、改、测试等），中间用 `report_task(status="working")` 汇报进展。
- 收敛只在 `report_task(status="done/failed")` **之后**生效。
- 主控可基于 worker 汇报**连续派发后续任务**实现多轮协作。
- 流程：派发 Task1 → worker 完成汇报 → 主控派发 Task2 → … → 全部完成 → 汇总给用户。

---

## 5. 模型选择建议

| 角色 | 推荐模型 | 原因 |
|---|---|---|
| 主控（需要强循环+严格遵循） | Claude Sonnet / Opus | 对 rule 遵循率 >95%，适合长链路 |
| Worker（单次任务） | GPT-5 / Claude 皆可 | 漏调影响小，可按成本选 |
| 纯代码生成 | 按项目偏好 | 无强制 |

> 在 Cursor 里切换模型不会改变本规则文本，但实际遵循率会有差别。关键路径建议用 Claude。
