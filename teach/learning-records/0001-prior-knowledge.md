# Prior knowledge (session bootstrap)

用户已建立：尺寸标注真值在 PML `markpipe`；主链路 `isobran → isoline → isoDim → lindim`；默认走 PolarSystem；弯头切段**不**靠 `type == 'ELBO'` 字符串。下一课应从「方向变化入口」入手，避免重复讲全链路。

---

# 0001 已交付，进入仿真

已开课「弯头何时切开」并应学员要求续开「手推 allarr」。后续 ZPD：能独立改玩具 bran 条件后重推段数，再进入 `putIntoIsoLine` 归段。
